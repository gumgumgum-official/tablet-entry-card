/**
 * 캔버스·SVG 공통 잉크 스타일 (둥근 이음 + 둥근 끝)
 */
export const INK_LINE_CAP: CanvasLineCap = "round";
export const INK_LINE_JOIN: CanvasLineJoin = "round";
export const INK_MITER_LIMIT = 5;

/** SVG stroke-* 속성 문자열 */
export const INK_SVG_STROKE_ATTRS = `stroke-linecap="round" stroke-linejoin="round"`;
